{
  "version": 2,
  "buildCommand": "cd ../ui-shared && npm ci && npm run build && cd ../api-clients && npm ci && npm run build && cd ../admin-web && npm ci && npm run build",
  "installCommand": "echo 'Dependencies installed during build'",
  "framework": "nextjs",
  "outputDirectory": ".next"
}

